dependencies:
  pre:
    - go get -v -u -f github.com/cee-dub/go-junit-report

test:
  override:
    - mkdir -p $CIRCLE_TEST_REPORTS/junit
    - go run build/cmd/zonedb/main.go
    - "(set -o pipefail && go test -v ./... -bench . -race | go-junit-report -dir $CIRCLE_TEST_REPORTS/junit)"
